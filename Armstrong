#include<iostream>
using namespace std;
int main(){
    int n;
    cin>>n;
   int dum=n;
   int sum=0;
    while(n>0){
        int ld=n%10;
        sum=sum+(ld*ld*ld);
        n=n/10;
    }
    if(sum==dum)cout<<"true";
    else
    cout<<"flase";
    return 0;
}

OUT PUT
153
true

=== Code Execution Successful ===
